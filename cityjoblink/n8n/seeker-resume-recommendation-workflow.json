{
  "name": "Seeker Resume Recommendation",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "seeker-resume-recommendation",
        "options": {}
      },
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2.1,
      "position": [
        260,
        120
      ],
      "id": "b6f9f2e1-3a7d-4c7b-9d3e-111111111111",
      "name": "Webhook1"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT id, title, company, location, required_skills, description, educational_attainment_required, status\nFROM jobs_catalog\nWHERE status = 'Open'\nORDER BY created_at DESC\nLIMIT 200;",
        "options": {}
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        500,
        120
      ],
      "id": "76a8d78f-9b85-4c9d-bfff-222222222222",
      "name": "Get Open Jobs",
      "credentials": {
        "postgres": {
          "id": "3t9IN3420wnFrcDg",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const webhookItem = $('Webhook1').first()?.json ?? {};\nconst payload = webhookItem.body ?? webhookItem;\nconst event = String(payload?.event || '').toLowerCase();\n\nif (event !== 'seeker_resume_uploaded') {\n  return [];\n}\n\nconst MIN_RECOMMENDATION_SCORE = 50;\n\nconst seekerEmail = String(payload.email || '').trim();\nif (!seekerEmail) {\n  throw new Error('Missing seeker email in payload');\n}\n\nconst seekerUserId = Number(payload.user_id || 0);\nif (!Number.isFinite(seekerUserId) || seekerUserId <= 0) {\n  throw new Error('Missing or invalid user_id in payload');\n}\n\nconst seekerName = String(payload.name || 'Jobseeker').trim();\nconst seekerEducationRaw = String(payload.educational_attainment || '').trim();\nconst seekerSkillsRawInput = payload.parsed_skill ?? [];\nconst resumeTextRaw = String(payload.resume_text || '');\n\nconst WEIGHTS = {\n  exact: 1.0,\n  alias: 0.85,\n  parent_group: 0.8,\n  grammar: 0.7,\n};\n\nconst normalizeText = (value) => String(value || '')\n  .normalize('NFKD')\n  .replace(/[\\u0300-\\u036f]/g, '')\n  .toLowerCase()\n  .replace(/[^a-z0-9+#./&\\s-]/g, ' ')\n  .replace(/\\s+/g, ' ')\n  .trim();\n\nconst escapeRegExp = (value) => String(value || '').replace(/[.*+?^{}()|[\\]\\\\]/g, '\\\\$&');\n\nconst containsPhrase = (haystack, phrase) => {\n  const normalizedHaystack = normalizeText(haystack);\n  const normalizedPhrase = normalizeText(phrase);\n  if (!normalizedHaystack || !normalizedPhrase) {\n    return false;\n  }\n\n  const pattern = new RegExp('(^|\\\\s)' + escapeRegExp(normalizedPhrase).replace(/\\\\ /g, '\\\\s+') + '(?=\\\\s|$)');\n  return pattern.test(normalizedHaystack);\n};\n\nconst tokenize = (text) => normalizeText(text).split(/\\s+/).filter(Boolean);\n\nconst stemTokenAggressive = (token) => {\n  let stem = normalizeText(token);\n  if (stem.length <= 2) {\n    return stem;\n  }\n\n  const suffixRules = [\n    ['izations', 'ize'],\n    ['ization', 'ize'],\n    ['ational', 'ate'],\n    ['ations', 'ate'],\n    ['ation', 'ate'],\n    ['ments', 'ment'],\n    ['ment', ''],\n    ['ingly', ''],\n    ['edly', ''],\n    ['ings', ''],\n    ['ing', ''],\n    ['ed', ''],\n    ['izers', 'ize'],\n    ['izer', 'ize'],\n    ['ers', 'er'],\n    ['er', ''],\n    ['ies', 'y'],\n    ['ied', 'y'],\n    ['s', ''],\n  ];\n\n  for (const [suffix, replacement] of suffixRules) {\n    if (stem.endsWith(suffix) && stem.length - suffix.length >= 3) {\n      stem = stem.slice(0, -suffix.length) + replacement;\n      break;\n    }\n  }\n\n  return stem;\n};\n\nconst parseSkillArray = (raw) => {\n  let value = raw;\n  if (typeof value === 'string') {\n    try {\n      value = JSON.parse(value);\n    } catch {\n      value = [value];\n    }\n  }\n\n  if (!Array.isArray(value) && typeof value === 'object' && value !== null) {\n    value = Object.values(value);\n  }\n\n  if (!Array.isArray(value)) {\n    value = [];\n  }\n\n  return value.map((item) => String(item || '').trim()).filter(Boolean);\n};\n\nconst parseParentheticalSkill = (skill) => {\n  const raw = String(skill || '');\n  const groups = [...raw.matchAll(/\\(([^)]+)\\)/g)]\n    .map((match) => String(match[1] || '').trim())\n    .filter(Boolean);\n\n  const base = raw.replace(/\\([^)]*\\)/g, ' ').replace(/\\s+/g, ' ').trim();\n  const children = [];\n\n  for (const group of groups) {\n    const parts = group\n      .split(/,|;|\\band\\b|&/i)\n      .map((piece) => piece.trim())\n      .filter(Boolean);\n\n    for (const piece of parts) {\n      children.push(piece);\n    }\n  }\n\n  return {\n    raw,\n    base,\n    children,\n  };\n};\n\nconst expandSlashVariants = (value) => {\n  const text = String(value || '').trim();\n  if (!text) {\n    return [];\n  }\n\n  const variants = new Set([text]);\n  if (text.includes('/')) {\n    text\n      .split('/')\n      .map((part) => part.trim())\n      .filter(Boolean)\n      .forEach((part) => variants.add(part));\n  }\n\n  return Array.from(variants);\n};\n\nconst uniqueNormalized = (values) => {\n  const output = [];\n  const seen = new Set();\n\n  for (const value of values) {\n    const normalized = normalizeText(value);\n    if (!normalized || seen.has(normalized)) {\n      continue;\n    }\n\n    seen.add(normalized);\n    output.push(normalized);\n  }\n\n  return output;\n};\n\nconst buildAliasIndex = (dynamicAliases) => {\n  const builtIn = {\n    javascript: ['js', 'java script'],\n    'node.js': ['nodejs', 'node js', 'node'],\n    typescript: ['ts'],\n    'c#': ['c sharp', 'csharp'],\n    'c++': ['cpp', 'c plus plus'],\n    mysql: ['my sql'],\n    postgresql: ['postgres', 'postgre sql'],\n    mongodb: ['mongo', 'mongo db'],\n    'project management': ['pm', 'project manager', 'project managers'],\n    'quality assurance': ['qa'],\n    'user interface design': ['ui design', 'ui ux', 'ux ui'],\n    networking: ['tcp ip', 'dns', 'vpn'],\n    'web development': ['html css', 'frontend development', 'front end development'],\n    'database management': ['db management', 'database administration', 'dba'],\n  };\n\n  const map = new Map();\n\n  const addAlias = (canonical, alias) => {\n    const canonicalKey = normalizeText(canonical);\n    const aliasKey = normalizeText(alias);\n    if (!canonicalKey || !aliasKey) {\n      return;\n    }\n\n    if (!map.has(canonicalKey)) {\n      map.set(canonicalKey, new Set());\n    }\n\n    map.get(canonicalKey).add(aliasKey);\n  };\n\n  for (const [canonical, aliases] of Object.entries(builtIn)) {\n    aliases.forEach((alias) => addAlias(canonical, alias));\n  }\n\n  const queue = Array.isArray(dynamicAliases) ? [...dynamicAliases] : [dynamicAliases];\n  while (queue.length > 0) {\n    const entry = queue.shift();\n    if (!entry) {\n      continue;\n    }\n\n    if (Array.isArray(entry)) {\n      queue.push(...entry);\n      continue;\n    }\n\n    if (typeof entry === 'string') {\n      const parts = entry.split('=>').map((part) => part.trim()).filter(Boolean);\n      if (parts.length === 2) {\n        addAlias(parts[0], parts[1]);\n      }\n      continue;\n    }\n\n    if (typeof entry !== 'object') {\n      continue;\n    }\n\n    if (entry.canonical && entry.alias) {\n      addAlias(entry.canonical, entry.alias);\n    }\n\n    if (entry.canonical && Array.isArray(entry.aliases)) {\n      entry.aliases.forEach((alias) => addAlias(entry.canonical, alias));\n    }\n\n    if (entry.skill && Array.isArray(entry.aliases)) {\n      entry.aliases.forEach((alias) => addAlias(entry.skill, alias));\n    }\n\n    if (!entry.canonical && !entry.skill) {\n      for (const [canonical, aliases] of Object.entries(entry)) {\n        if (Array.isArray(aliases)) {\n          aliases.forEach((alias) => addAlias(canonical, alias));\n        } else {\n          addAlias(canonical, aliases);\n        }\n      }\n    }\n  }\n\n  return map;\n};\n\nconst buildSkillSpec = (requiredSkill, aliasIndex) => {\n  const parsed = parseParentheticalSkill(requiredSkill);\n  const canonical = normalizeText(parsed.base || parsed.raw);\n  const children = uniqueNormalized(parsed.children.flatMap(expandSlashVariants));\n\n  const aliasCandidates = new Set();\n  aliasCandidates.add(canonical);\n  aliasCandidates.add(canonical.replace(/\\./g, ''));\n  aliasCandidates.add(canonical.replace(/\\s+/g, ''));\n\n  if (aliasIndex.has(canonical)) {\n    for (const alias of aliasIndex.get(canonical)) {\n      aliasCandidates.add(alias);\n    }\n  }\n\n  const aliases = uniqueNormalized(Array.from(aliasCandidates));\n  const grammarTerms = uniqueNormalized([canonical, ...aliases, ...children]);\n\n  return {\n    raw: String(requiredSkill || '').trim(),\n    canonical,\n    aliases,\n    children,\n    grammarTerms,\n  };\n};\n\nconst buildEvidenceContext = (resumeText, parsedSkills) => {\n  const resumeNormalized = normalizeText(resumeText);\n  const parsedNormalized = uniqueNormalized(parsedSkills);\n  const parsedSet = new Set(parsedNormalized);\n\n  const combinedTokens = [\n    ...tokenize(resumeNormalized),\n    ...parsedNormalized.flatMap((skill) => tokenize(skill)),\n  ];\n\n  const stemSet = new Set(combinedTokens.map((token) => stemTokenAggressive(token)).filter(Boolean));\n\n  return {\n    resumeNormalized,\n    parsedSet,\n    stemSet,\n  };\n};\n\nconst evaluateSkillMatch = (spec, context) => {\n  const exactMatch = [spec.canonical].find((term) => term && (containsPhrase(context.resumeNormalized, term) || context.parsedSet.has(term)));\n  if (exactMatch) {\n    return { matched: true, weight: WEIGHTS.exact, type: 'exact', term: exactMatch };\n  }\n\n  const aliasMatch = spec.aliases.find((term) => term && (containsPhrase(context.resumeNormalized, term) || context.parsedSet.has(term)));\n  if (aliasMatch) {\n    return { matched: true, weight: WEIGHTS.alias, type: 'alias', term: aliasMatch };\n  }\n\n  const parentGroupMatch = spec.children.find((term) => term && (containsPhrase(context.resumeNormalized, term) || context.parsedSet.has(term)));\n  if (parentGroupMatch) {\n    return { matched: true, weight: WEIGHTS.parent_group, type: 'parent_group', term: parentGroupMatch };\n  }\n\n  const grammarMatch = spec.grammarTerms.find((term) => {\n    const tokens = tokenize(term);\n    if (tokens.length === 0) {\n      return false;\n    }\n    return tokens.every((token) => context.stemSet.has(stemTokenAggressive(token)));\n  });\n\n  if (grammarMatch) {\n    return { matched: true, weight: WEIGHTS.grammar, type: 'grammar', term: grammarMatch };\n  }\n\n  return { matched: false, weight: 0, type: null, term: null };\n};\n\nconst normalizeNoSymbols = (value) => normalizeText(value).replace(/[^a-z0-9\\s]/g, ' ').replace(/\\s+/g, ' ').trim();\n\nconst educationLevels = [\n  { level: 'doctorate', rank: 5, keywords: ['doctorate', 'doctoral', 'phd', 'doctor of philosophy'] },\n  { level: 'masters', rank: 4, keywords: ['master', 'masters', \"master's\", 'm.s', 'ms', 'm.a', 'ma', 'mba'] },\n  { level: 'bachelors', rank: 3, keywords: ['bachelor', 'bachelors', \"bachelor's\", 'b.s', 'bs', 'b.a', 'ba', 'college graduate', 'college grad', 'degree'] },\n  { level: 'associate', rank: 2, keywords: ['associate degree', 'associate'] },\n  { level: 'vocational', rank: 2, keywords: ['vocational', 'tesda', 'certificate', 'technical-vocational', 'nc ii', 'nc iii'] },\n  { level: 'highschool', rank: 1, keywords: ['high school', 'secondary', 'senior high', 'shs'] },\n];\n\nconst inferEducationLevel = (value) => {\n  const normalized = normalizeNoSymbols(value);\n  if (!normalized || ['any', 'not specified', 'none', 'n a', 'na'].includes(normalized)) {\n    return null;\n  }\n\n  for (const item of educationLevels) {\n    if (item.keywords.some((keyword) => normalized.includes(normalizeNoSymbols(keyword)))) {\n      return item.level;\n    }\n  }\n\n  return null;\n};\n\nconst levelRank = (level) => {\n  const found = educationLevels.find((item) => item.level === level);\n  return found ? found.rank : null;\n};\n\nlet seekerEducationLevel = inferEducationLevel(seekerEducationRaw);\nif (!seekerEducationLevel) {\n  seekerEducationLevel = inferEducationLevel(resumeTextRaw);\n}\n\nconst seekerSkillsRaw = parseSkillArray(seekerSkillsRawInput);\nconst seekerContext = buildEvidenceContext(resumeTextRaw, seekerSkillsRaw);\n\nconst jobs = $input.all().map((item) => item.json);\nconst scored = [];\nconst disqualified = [];\nconst allMatches = [];\n\nfor (const job of jobs) {\n  const title = String(job.title || '');\n  const company = String(job.company || '');\n  const location = String(job.location || '');\n  const requiredEducationRaw = String(job.educational_attainment_required || '').trim();\n\n  const requiredEducationLevel = inferEducationLevel(requiredEducationRaw);\n  const isEducationRequired = (() => {\n    const normalized = normalizeText(requiredEducationRaw);\n    return normalized !== '' && !['any', 'not specified', 'none', 'n/a', 'na'].includes(normalized);\n  })();\n\n  if (isEducationRequired) {\n    let educationQualified = false;\n\n    if (requiredEducationLevel && seekerEducationLevel) {\n      educationQualified = Number(levelRank(seekerEducationLevel) || 0) >= Number(levelRank(requiredEducationLevel) || 0);\n    } else {\n      const seekerNormalized = normalizeNoSymbols(seekerEducationRaw);\n      const requiredNormalized = normalizeNoSymbols(requiredEducationRaw);\n      educationQualified = seekerNormalized !== '' && (\n        seekerNormalized === requiredNormalized ||\n        seekerNormalized.includes(requiredNormalized)\n      );\n    }\n\n    if (!educationQualified) {\n      const seekerEducationLabel = seekerEducationRaw || 'Not specified';\n      const reason = 'Auto-disqualified: requires ' + requiredEducationRaw + ', applicant has ' + seekerEducationLabel + '.';\n\n      const disqualifiedItem = {\n        job_id: Number(job.id),\n        title,\n        company,\n        location,\n        score: 0,\n        reasons: reason,\n        disqualified: true,\n      };\n\n      disqualified.push(disqualifiedItem);\n      allMatches.push(disqualifiedItem);\n      continue;\n    }\n  }\n\n  const requiredSkillsRaw = parseSkillArray(job.required_skills ?? []);\n  const aliasIndex = buildAliasIndex([\n    payload.skill_aliases ?? null,\n    job.skill_aliases ?? null,\n    job.required_skill_aliases ?? null,\n  ]);\n  const requiredSpecs = requiredSkillsRaw.map((skill) => buildSkillSpec(skill, aliasIndex));\n\n  let score = 0;\n  const reasons = [];\n\n  let matchedRequiredCount = 0;\n  let weightedSkillHits = 0;\n  const matchTypeCounts = { exact: 0, alias: 0, parent_group: 0, grammar: 0 };\n\n  for (const spec of requiredSpecs) {\n    const result = evaluateSkillMatch(spec, seekerContext);\n    if (!result.matched) {\n      continue;\n    }\n\n    matchedRequiredCount += 1;\n    weightedSkillHits += result.weight;\n    if (result.type && matchTypeCounts[result.type] !== undefined) {\n      matchTypeCounts[result.type] += 1;\n    }\n  }\n\n  if (requiredSpecs.length > 0) {\n    const skillScore = (weightedSkillHits / requiredSpecs.length) * 70;\n    score += skillScore;\n    reasons.push(\n      'skills ' + matchedRequiredCount + '/' + requiredSpecs.length +\n      ' | weighted=' + weightedSkillHits.toFixed(2) +\n      ' | exact=' + matchTypeCounts.exact +\n      ', alias=' + matchTypeCounts.alias +\n      ', parent_group=' + matchTypeCounts.parent_group +\n      ', grammar=' + matchTypeCounts.grammar\n    );\n  }\n\n  const titleKeywords = tokenize(title).filter((word) => word.length > 3);\n  const titleHits = titleKeywords.filter((keyword) => containsPhrase(seekerContext.resumeNormalized, keyword)).length;\n  if (titleKeywords.length > 0 && titleHits > 0) {\n    const titleScore = (titleHits / titleKeywords.length) * 30;\n    score += titleScore;\n    reasons.push('title alignment ' + titleHits + '/' + titleKeywords.length);\n  }\n\n  const finalScore = Math.round(Math.min(score, 100));\n\n  const matchItem = {\n    job_id: Number(job.id),\n    title,\n    company,\n    location,\n    score: finalScore,\n    reasons: reasons.join(' | ') || 'General profile fit',\n    disqualified: false,\n  };\n\n  allMatches.push(matchItem);\n\n  if (finalScore >= MIN_RECOMMENDATION_SCORE) {\n    scored.push(matchItem);\n  }\n}\n\nscored.sort((a, b) => Number(b.score) - Number(a.score));\nconst top = scored.slice(0, 5);\n\nif (allMatches.length === 0) {\n  return [];\n}\n\nconst recommendationListHtml = top.length > 0\n  ? '<ul>' + top.map((r) => '<li><b>' + r.title + '</b> - ' + r.company + ' (' + r.location + ') <b>' + r.score + '%</b><br/><small>' + r.reasons + '</small></li>').join('') + '</ul>'\n  : '<p>No jobs passed resume-fit screening at the moment.</p>';\n\nconst disqualifiedListHtml = disqualified.length > 0\n  ? '<br/><p><b>Not qualified due to education requirement:</b></p><ul>' + disqualified.map((r) => '<li><b>' + r.title + '</b> - requires higher educational attainment.<br/><small>' + r.reasons + '</small></li>').join('') + '</ul>'\n  : '';\n\nlet emailSubject = 'Your CityJobLink screening results';\nif (top.length === 0 && disqualified.length > 0) {\n  emailSubject = 'Not Qualified for Current Open Jobs';\n}\n\nconst emailHtml = 'Hi ' + seekerName + ',<br/><br/>' + (top.length > 0 ? 'Here are your top recommended jobs based on your resume:' : 'We screened current openings using your resume.') + '<br/><br/>' + recommendationListHtml + disqualifiedListHtml + '<br/><br/>Log in to CityJobLink to view details and apply to qualified jobs.<br/><br/>Best regards,<br/>CityJobLink Team';\n\nreturn [{\n  json: {\n    event: 'seeker_resume_uploaded',\n    seeker_user_id: seekerUserId,\n    to_email: seekerEmail,\n    seeker_name: seekerName,\n    recommendations: top,\n    disqualified_jobs: disqualified,\n    all_matches: allMatches,\n    email_subject: emailSubject,\n    email_html: emailHtml,\n  }\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        760,
        120
      ],
      "id": "9a3ef532-8aa8-4e14-9f03-333333333333",
      "name": "Score Jobs"
    },
    {
      "parameters": {
        "jsCode": "const payload = $json;\nconst seekerUserId = Number(payload.seeker_user_id || 0);\nif (!Number.isFinite(seekerUserId) || seekerUserId <= 0) {\n  return [];\n}\n\nconst matches = Array.isArray(payload.all_matches) ? payload.all_matches : [];\n\nreturn matches.map((match) => ({\n  json: {\n    job_id: Number(match.job_id),\n    user_id: seekerUserId,\n    match_score: Number(match.score || 0),\n    match_reasons: String(match.reasons || '')\n  }\n}));"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1020,
        280
      ],
      "id": "5e9e4f3b-ef0f-4b8a-8f18-555555555555",
      "name": "Prepare Match Rows"
    },
    {
      "parameters": {
        "jsCode": "const payload = $input.first()?.json ?? {};\nconst recommendations = Array.isArray(payload.recommendations) ? payload.recommendations : [];\nreturn recommendations.length > 0 ? [{ json: payload }] : [];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1020,
        40
      ],
      "id": "2ebeffc1-34be-4bb6-8319-8f90386fffe7",
      "name": "Filter Email 50+"
    },
    {
      "parameters": {
        "schema": {
          "__rl": true,
          "mode": "list",
          "value": "public"
        },
        "table": {
          "__rl": true,
          "value": "job_matches",
          "mode": "list",
          "cachedResultName": "job_matches"
        },
        "columns": {
          "mappingMode": "defineBelow",
          "value": {
            "job_id": "={{ Number($json.job_id) }}",
            "user_id": "={{ Number($json.user_id) }}",
            "match_score": "={{ Number($json.match_score) }}",
            "match_reasons": "={{ $json.match_reasons }}"
          },
          "schema": [
            {
              "id": "job_id",
              "displayName": "job_id",
              "required": true,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": true
            },
            {
              "id": "user_id",
              "displayName": "user_id",
              "required": true,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": true
            },
            {
              "id": "match_score",
              "displayName": "match_score",
              "required": true,
              "defaultMatch": false,
              "display": true,
              "type": "number",
              "canBeUsedToMatch": true
            },
            {
              "id": "match_reasons",
              "displayName": "match_reasons",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true
            }
          ],
          "attemptToConvertTypes": true,
          "convertFieldsToString": false
        },
        "options": {}
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        1240,
        280
      ],
      "id": "68f7d9cd-2a1b-4f4a-bfd0-666666666666",
      "name": "Store Matches",
      "credentials": {
        "postgres": {
          "id": "3t9IN3420wnFrcDg",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "fromEmail": "cityjoblink.direct@gmail.com",
        "toEmail": "={{ $json.to_email }}",
        "subject": "={{ $json.email_subject }}",
        "emailFormat": "html",
        "html": "={{ $json.email_html }}",
        "options": {}
      },
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2.1,
      "position": [
        1020,
        120
      ],
      "id": "4f0a0ad9-3bba-4f00-a9dd-444444444444",
      "name": "Send Recommendation Email",
      "continueOnFail": true,
      "credentials": {
        "smtp": {
          "id": "fJ6zTRRaEJz5Zvw9",
          "name": "SMTP account"
        }
      }
    }
  ],
  "pinData": {},
  "connections": {
    "Webhook1": {
      "main": [
        [
          {
            "node": "Get Open Jobs",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Open Jobs": {
      "main": [
        [
          {
            "node": "Score Jobs",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Score Jobs": {
      "main": [
        [
          {
            "node": "Prepare Match Rows",
            "type": "main",
            "index": 0
          },
          {
            "node": "Filter Email 50+",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Filter Email 50+": {
      "main": [
        [
          {
            "node": "Send Recommendation Email",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Match Rows": {
      "main": [
        [
          {
            "node": "Store Matches",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate",
    "availableInMCP": false
  },
  "tags": []
}

